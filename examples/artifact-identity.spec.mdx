---
title: Rail Artifact Identity Contract
description: Example ContextKit spec for Rail artifact identity and projection authority.
docType: spec
status: draft
owner: rail-runtime
appliesTo:
  - docs/SPEC.md
  - src/rail/artifacts/
  - src/rail/supervisor/
agentEntryPoints:
  - docs/SPEC.md
  - docs/ARCHITECTURE.md
  - docs/CONVENTIONS.md
validationCommands:
  - uv run --python 3.12 pytest -q
  - uv run --python 3.12 ruff check src tests
  - uv run --python 3.12 mypy src/rail
lastUpdated: 2026-05-18
---

import { Aside, Card, CardGrid } from '@astrojs/starlight/components';

# Rail Artifact Identity Contract

<Aside type="danger" title="Canonical Contract">
This example shows how a Rail contract can be represented with ContextKit.
</Aside>

## Human Summary

Rail work is identified by artifact handles, not request files or inferred task
names. Users should be able to resume or inspect work through durable artifacts.

## Agent Summary

When changing artifact identity, update runtime contracts, tests, and any skill
surface that describes start, resume, retry, status, or result behavior.

## Source Of Truth

The canonical source of truth remains the Rail product spec. This example is a
ContextKit style sample, not active Rail product authority.

## Goal

Show how a durable runtime contract can name its identity rules, owned
surfaces, validation, and agent instructions in one plain MDX document.

## Architecture

Rail separates request drafting, artifact persistence, supervision, and result
projection. Artifact identity belongs to the persisted artifact layer, while
supervision and result projection consume validated handles.

## Problem

Artifact identity rules are easy to blur when plans mention request files,
target-local state, or generated output. That can make agents infer the wrong
resume or projection authority.

## Scope

### In

- Artifact handle identity
- Resume and retry semantics
- Result projection authority

### Out

- Downstream application behavior
- Provider-specific event formats

## Non-Goals

- This example does not replace Rail's canonical docs.
- This example does not define new runtime behavior.

## File Or Surface Map

| Surface | Role | Owner |
| --- | --- | --- |
| `docs/SPEC.md` | Canonical product contract | rail-runtime |
| `src/rail/artifacts/` | Artifact persistence and identity | rail-runtime |
| `src/rail/supervisor/` | Supervision over artifact handles | rail-runtime |

## Vocabulary

| Term | Meaning |
| --- | --- |
| Artifact handle | Durable identity returned by Rail for task or goal work |
| Projection | Status or result read from persisted artifacts |

## Contract

The system must preserve these rules:

- Fresh work creates a fresh artifact handle.
- Existing work resumes only from a validated handle.
- Result projection reads artifacts only.

## Interfaces

### Public Surface

```python
rail.start_task(draft)
rail.supervise(handle)
rail.result(handle)
```

### Internal Surface

- Artifact store
- Supervisor
- Evaluator gate

## Evidence

- Existing Rail docs distinguish product spec, architecture, and conventions.
- Runtime API examples expose `start_task`, `supervise`, and `result` around durable handles.

## Invariants

<CardGrid>
  <Card title="Identity" icon="document">
    Request files are not durable run identity.
  </Card>
  <Card title="Authority" icon="check-circle">
    Terminal success comes from gated artifacts.
  </Card>
</CardGrid>

## Validation

```bash
uv run --python 3.12 pytest -q
uv run --python 3.12 ruff check src tests
uv run --python 3.12 mypy src/rail
```

## Agent Instructions

- Read Rail's canonical spec before editing runtime behavior.
- Keep skill-facing workflow text aligned with the Python API.
- Do not infer terminal success from provider output.

## Review Checklist

- Artifact handle rules are stated as contract bullets, not only prose.
- Public and internal surfaces both identify the owner of identity behavior.
- Validation commands cover runtime tests, lint, and type checks.

## Open Questions

- TBD
